<h1>Listing mains</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @mains.each do |main| %>
  <tr>
    <td><%= link_to 'Show', main %></td>
    <td><%= link_to 'Edit', edit_main_path(main) %></td>
    <td><%= link_to 'Destroy', main, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Main', new_main_path %>
